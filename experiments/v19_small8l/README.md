# V19 — standalone 8-layer model

The same-tokenizer eight-layer V4 draft was tested as a standalone model with top-2 routing. It reaches **132.89 tok/s single-stream** but scores **0/6** on the standard smoke test. The speed target is achievable only after distillation restores useful behavior.
